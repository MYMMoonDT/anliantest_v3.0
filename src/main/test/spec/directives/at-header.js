'use strict';

describe('Directive: atHeader', function () {

  // load the directive's module
  beforeEach(module('anliantestApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
